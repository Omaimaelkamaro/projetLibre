spring:
  cloud:
    gateway:
      routes:
        - id: userAuthentification
          uri: lb://userAuthentification
          predicates:
            - Path=/auth/**

  application:
    name: GATEWAY

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8081
